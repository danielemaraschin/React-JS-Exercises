
const person = {
    name: 'Dani',
    age: 30
}

const {age} = person
console.log(age)

const idade = person.age
console.log(idade)

const nome = person.name
console.log(nome)

const{name} = person
console.log(name)